import 'package:flutter/material.dart';

class PaddindUtility {
  final paddingTop = const EdgeInsets.only(top: 8);

  final paddingCard = const EdgeInsets.all(20.0);
  final paddingList = const EdgeInsets.all(8.0);
  final paddingHorizontal = const EdgeInsets.symmetric(horizontal: 20);
}
